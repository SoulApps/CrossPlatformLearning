capital = input("Mete dinero ")
cuotas = input("Mete cuotas ")
intereses = float(capital) * float(1.05)
importeTotal = float(capital) + float(intereses)

print("Importe total:", importeTotal, "Cuotas:", cuotas)
